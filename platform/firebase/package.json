{
  "name": "@lyfelabz/firebase-config",
  "version": "0.0.0",
  "private": true,
  "description": "LyfeLabz Firebase configuration and Firestore Rules tests.",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"jest\"",
    "test:rules:watch": "firebase emulators:exec --only firestore \"jest --watch\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/jest": "^29.5.12",
    "firebase": "^10.12.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "typescript": "^5.4.5"
  }
}
